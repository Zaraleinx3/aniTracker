import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      maxWidth: 200,
      float: 'left',
      marginLeft: '18px',
      marginBottom: '18px',
    },
  });

export default useStyles;